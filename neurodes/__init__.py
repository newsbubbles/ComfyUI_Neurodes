"""neurodes — neural network architecture as a ComfyUI node graph.

``neurodes.core`` is plain Python and torch, with no ComfyUI imports, so it can be tested
and used on its own. ``neurodes.nodes`` is the thin adapter that exposes it to ComfyUI.
"""

__version__ = "0.1.0"
